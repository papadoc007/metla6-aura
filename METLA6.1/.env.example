# TeamPulse / METLA6.1 — environment template.
# Copy this file to .env and fill in real values. Never commit .env.

# Seedance 2.0 (Python fallback pipeline; not used by Remotion render).
SEEDANCE_API_KEY=

# Suno (soundtrack generation).
SUNO_API_KEY=
SUNO_API_BASE=https://api.sunoapi.org/api/v1
